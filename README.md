# Virtual Machine management with Vagrant

Run machine

```bash
vagrant up
```

Provision scripts

```bash
vagrant provision
```

Audit SSH config

```bash 
vagrant ssh-config
```

The machine is accessible with the following credentials

```
user: vagrant
password: vagrant
```